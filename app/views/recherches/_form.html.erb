<%= form_for(@recherch) do |f| %>
  <% if @recherch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recherch.errors.count, "error") %> prohibited this recherch from being saved:</h2>

      <ul>
      <% @recherch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
